# Development and test dependencies. Installed into the host venv by
# mcp/setup/setup_host.sh; deliberately NOT installed into the server image, so
# pytest/playwright/openai don't ship to production.
-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
playwright==1.61.0

# Used only by mcp/tests/test_e2e_llm_tool_loop.py, which drives the OpenAI API
# directly to verify tool dispatch. The server itself makes no LLM calls.
openai==2.46.0
