mcp>=0.1.0
fastmcp>=0.1.0
mcpo>=0.0.20
pydantic>=2.0.0
# openai is no longer used by the server itself (the LLM segment fetcher was removed).
# Still required by mcp/tests/test_e2e_llm_tool_loop.py, which drives Open WebUI's
# OpenAI-compatible endpoint. Splitting test deps into requirements-dev.txt is tracked
# in mcp/TECH_DEBT.md.
openai>=1.0.0
requests>=2.31.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
playwright>=1.40.0
