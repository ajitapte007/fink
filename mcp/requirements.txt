# Runtime dependencies for the MCP server image (mcp/setup/Dockerfile.mcp).
# Test-only packages live in requirements-dev.txt so they don't ship to production.
#
# Pinned to the versions the suite was last verified green against. `mcp` and `fastmcp`
# especially: server.py carries a sys.path shim to stop the local mcp/ directory
# shadowing the mcp pip package, so an unexpected upgrade can break imports outright.
# Bump deliberately and re-run ./mcp/tests/verify.sh.
mcp==1.28.1
fastmcp==3.4.4
mcpo==0.0.20
pydantic==2.13.4
requests==2.34.2
